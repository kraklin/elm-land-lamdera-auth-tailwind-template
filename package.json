{
  "name": "groundschool",
  "version": "1.0.0",
  "description": "> How to use [Lamdera](https://lamdera.com/) and [Elm Land](https://elm.land) together!",
  "main": "index.js",
  "scripts": {
    "start": "run-pty % lamdera live % elm-land server % tailwindcss -i ./src/styles.css -o ./public/styles.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "elm-land": "^0.19.4",
    "tailwindcss": "^3.3.5"
  },
  "devDependencies": {
    "run-pty": "^4.1.0"
  }
}
